apiVersion: batch/v1
kind: Job
metadata:
  name: pytorch-ddp-training
  labels:
    app: pytorch-training
spec:
  # Use completions and parallelism to control the number of pods
  # For 4 GPUs across 2 nodes (2 GPUs per node), set completions to 2 and parallelism to 2
  completions: 2
  parallelism: 2
  completionMode: Indexed
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: pytorch-training
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      automountServiceAccountToken: false
      # IMPORTANT: subdomain must match the headless Service name for stable pod DNS
      # Service name in your repo: pytorch-training-headless
      subdomain: pytorch-training-headless

      restartPolicy: Never

      containers:
      - name: trainer
        image: pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        command: ["/bin/bash"]
        args:
        - -c
        - |
          set -euo pipefail
          # Deterministic rank from Indexed Job
          export RANK="${JOB_COMPLETION_INDEX}"

          # Master is always rank 0 (pod name is stable with completionMode: Indexed)
          export MASTER_PORT=29500
          export MASTER_ADDR="pytorch-ddp-training-0.pytorch-training-headless.${POD_NAMESPACE}.svc.cluster.local"

          export WORLD_SIZE="${WORLD_SIZE}"

          # Install required packages (non-root)
          export HOME=/tmp
          export PIP_CACHE_DIR=/tmp/pip-cache
          pip install -q --user torchvision tensorboard

          # Run training script
          python /workspace/train.py \
            --data-dir /data \
            --output-dir /output \
            --num-epochs "${NUM_EPOCHS}" \
            --batch-size "${BATCH_SIZE}" \
            --world-size "${WORLD_SIZE}" \
            --rank "${RANK}" \
            --master-addr "${MASTER_ADDR}" \
            --master-port "${MASTER_PORT}"
        env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace

        # Available on Indexed Jobs via annotation
        - name: JOB_COMPLETION_INDEX
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['batch.kubernetes.io/job-completion-index']

        - name: WORLD_SIZE
          value: "2"  # Must match completions

        - name: NUM_EPOCHS
          valueFrom:
            configMapKeyRef:
              name: training-config
              key: num_epochs

        - name: BATCH_SIZE
          valueFrom:
            configMapKeyRef:
              name: training-config
              key: batch_size

        resources:
          requests:
            cpu: "4"
            memory: "16Gi"
            nvidia.com/gpu: "2"
          limits:
            cpu: "4"
            memory: "16Gi"
            nvidia.com/gpu: "2"

        volumeMounts:
        - name: training-data
          mountPath: /data
        - name: output
          mountPath: /output
        - name: training-script
          mountPath: /workspace

      volumes:
      - name: training-data
        persistentVolumeClaim:
          claimName: training-data-pvc
      - name: output
        persistentVolumeClaim:
          claimName: training-output-pvc
      - name: training-script
        configMap:
          name: training-script
          defaultMode: 0755

      # Node selectors are the main difference among cloud providers
      # Uncomment the appropriate section for your cloud provider
      #
      # GKE
      # nodeSelector:
      #   cloud.google.com/gke-accelerator: nvidia-tesla-v100
      #   cloud.google.com/gke-gpu-driver-version: default
      #
      # EKS
      # nodeSelector:
      #   node.kubernetes.io/instance-type: p3.2xlarge
      #
      # AKS
      # nodeSelector:
      #   agentpiscasi.com/gpu: "true"  # Common label for AKS GPU nodes
